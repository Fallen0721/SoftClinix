@page "/gfamiliar"
@inherits LayoutComponentBase

<PageTitle>Grupo Familiar</PageTitle>

<form asp-action="Index" method="post" enctype="multipart/form-data">
    <nav class="navbar navbar-expand-lg navbar-light bg-black">
        <div class="container-fluid">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">Registro</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/estadopaciente">Paciente Datos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/gfamiliar">Grupo Familiar</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/D_Complement">Datos Complementarios</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container">
        <h1>Grupo Familiar</h1>

        

        <!-- Información del Paciente -->
        <div class="row">
            <div class="col-12">
                <label class="col-3 d-inline-block" for="Paciente">Paciente:</label>
                <label class="col-3 d-inline-block" for="IDpaciente">0081722</label>
                <label class="col-3 d-inline-block" for="NamePaciente">Edgar Jeovany Cruz Valderrama</label>
            </div>
        </div>
    </div>

    <!-- Tabla de Grupo Familiar -->
    <div class="container">
        <table class="table">
            <thead>
                <tr>
                    <th>Nombre Familiar</th>
                    <th>Parentesco</th>
                    <th>Condición</th>
                    <th>Habilitado</th>
                    <th>C-Asegurado</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                </tr>
            </tbody>
        </table>
    </div>
    <!-- Botones de navegación -->
    <div class="button-container">
        <a href="/estadopaciente" class="btn btn-secondary">Página anterior</a>
        <a href="/D_Complement" class="btn btn-primary">Siguiente página</a>
    </div>
</form>

@code {
    private string? NombrePaciente;
    private string? InNombre;

    private void GuardarGFamiliar()
    {
    //Mostrar Id y Nombre Paciente
    }

    private PacientesF[]? Familiares;

   

    public class PacientesF
    {
        public string? NombreF { get; set; }

        public string? Parentesco { get; set; }

        public string? Condicion { get; set; }

        public string? Habilitado { get; set; }

        public string? C_Asegurado { get; set; }

       
    }
}

<style>
    /* Esto arregla el problema de la barra */
    /* Ajuste de la barra de navegación */
    .navbar {
        position: relative;
        margin-left: 250px;
    }

    /* Contenedor principal */
    .container {
        max-width: 900px;
        background-color: #f9f9f9;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        margin-top: 20px;
    }

    /* Contenedor de botones de navegación */
    .button-container {
        display: flex;
        justify-content: space-between; /* Distribuye los botones en extremos opuestos */
        margin-top: 20px;
    }

    /* Estilo para los botones */
    .btn {
        padding: 10px 20px;
        font-size: 16px;
    }

    .btn-secondary {
        background-color: #6c757d;
        border-color: #6c757d;
        color: #fff;
    }

        .btn-secondary:hover {
            background-color: #5a6268;
            border-color: #545b62;
        }

</style>




